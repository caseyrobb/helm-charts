apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-notes-sync.fullname" . }}
  labels:
    {{- include "simple-notes-sync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-notes-sync.selectorLabels" . | nindent 4 }}
